import React from "react"

function Footer(props) {
    return (
        <footer className={`${props.theme}-theme`}>
            <p>A perfect footer</p>
        </footer>
    )
}

export default Footer